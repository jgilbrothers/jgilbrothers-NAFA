# Synthetic fixture suite

No real names, accounts, addresses, statements, or court records belong here. Generate fixtures with fictional parties `Alex Example` and `Jordan Example`, account suffix `0000`, and amounts under $100. Coverage required: searchable, scanned, mixed, rotated, faint, missing-page, and low-confidence PDFs; PNG/JPG receipts; TXT, CSV, DOCX, XLSX, unsupported bytes; duplicate bytes under a different name; December/January dates; ambiguous payment direction; inactive `0.00` columns; and separate allegation, finding, and ordered-obligation samples.
